# Kissne Android

KB1-ANDROID-CHAT §0.3.17 client. The app talks only to the frozen Mobile
Transport contract through the HTTPS base URL; it never contains provider
credentials or a second Conversation/Agent/Memory truth.

This commit intentionally contains the RED contract tests before the client
implementation.
